.star-div{class: 'd-flex justify-content-center align-items-center text-right'}
  %p= rating_value
  %p &nbsp
  - (1..5).to_a.each_with_index do |_value, index|
    - if rating_value < index + 1
      %i{class: "far fa-star stars"}
    - else
      %i{class: "fas fa-star stars"}
  %p &nbsp
  %p= link_to "(#{rating_count})", deck_reviews_path(deck)

